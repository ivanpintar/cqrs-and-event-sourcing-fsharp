module PinetreeShop.Domain.Orders.Tests.PrepareForShipping

let ``When PrepareForShipping ReadyForShipping`` = 0
let ``When PrepareForShipping no orders fail`` = 0
let ``When PrepareForShipping not pending fail`` = 0